// *PLACEHOLDER* — Churn triage screen
export function TriageBad() {
  return (
    <section>
      <h1>Churn Triage *PLACEHOLDER*</h1>
      <p>
        Risk signals: Stripe cancellations (major), usage drops (medium), negative support tickets
        (minor). Agents reach out first; escalate to a real CSM.
      </p>
    </section>
  );
}

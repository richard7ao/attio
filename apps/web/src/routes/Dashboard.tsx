// *PLACEHOLDER* — Dashboard screen
export function Dashboard() {
  return (
    <section>
      <h1>Dashboard *PLACEHOLDER*</h1>
      <p>Account analytics overview — MRR, seat usage, renewals, signal volume.</p>
    </section>
  );
}
